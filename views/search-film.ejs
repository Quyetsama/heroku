<%- include('partials/header'); -%>
    <div class="mt-4">
        <div class="row">

            <% for(var i=0; i<films.length; i++){ %>
            <div class="col-sm-6 col-lg-4">
                <div class="card" style="margin-top: 16px;">
                    <a href="/films/<%= films[i].slug %>/1"><img src="<%= films[i].imageUrl %>" class="card-img-top" alt="Card image cap"></a>
                    <div class="card-body">
                    <h5 class="card-title"><%= films[i].title %> - <%= films[i].episodes.length %> Tập</h5>
                    <p class="card-text"><%= films[i].category %></p>
                    <a href="/films/<%= films[i].slug %>/1" class="btn btn-primary">Xem Phim</a>
                    </div>
                  </div>
            </div>
            <% } %>
        </div>
    </div>

<%- include('partials/footer'); -%>